#include <iostream>

#include "Application.h"

// Objmgr has defines that show scenes, only scenes with word demo has WASD, camera mouse, and scroll
// Check out RenderDemoSpheres, finish it off by copying renderdemocubes with renderspheres code (mesh together)

int main()
{
	Application app;

	app.Start();

	return 0;
}